/* eslint-disable */

import Search from '@/components/users/Search.vue';

describe('Search', () => {
  test('If has found a User, shows the card', () => {
    expect(true).toBe(true);
  });

  test('If has no found a User, shows error message', () => {
    expect(true).toBe(true);
  });
});
